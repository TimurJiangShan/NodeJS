var _ = require('underscore');

// Core module
// File or foler
// node_modules

console.log(_.contains([1,3,2], 2))

// npm list 列出安装的依赖
// npm list --depth=0 列出第一层的依赖（像树一样）

var keke = require('keke-lib-742345');
console.log(keke.add(1,2));

/**
 * 1. npm adduser
 * 2. npm login
 * 3. npm publish(version 和 name一致)
 * 
*/